clear 
load('tdcells_a2h.mat')

%%
td_a = [td_a_ft{:}];
num = size(td_a_ft,2);
tdlen_a = zeros(1,num);
for idx = 1:num
    tdlen_a(idx) = size(td_a_ft{idx},2);
end

save('tdarr_a2h','td_a','tdlen_a');

%%
td_b = [td_b_ft{:}];
num = size(td_b_ft,2);
tdlen_b = zeros(1,num);
for idx = 1:num
    tdlen_b(idx) = size(td_b_ft{idx},2);
end

save('tdarr_a2h','td_b','tdlen_b','-append');

%%
td_c = [td_c_ft{:}];
num = size(td_c_ft,2);
tdlen_c = zeros(1,num);
for idx = 1:num
    tdlen_c(idx) = size(td_c_ft{idx},2);
end

save('tdarr_a2h','td_c','tdlen_c','-append');

%%
td_d = [td_d_ft{:}];
num = size(td_d_ft,2);
tdlen_d = zeros(1,num);
for idx = 1:num
    tdlen_d(idx) = size(td_d_ft{idx},2);
end

save('tdarr_a2h','td_d','tdlen_d','-append');

%%
td_e = [td_e_ft{:}];
num = size(td_e_ft,2);
tdlen_e = zeros(1,num);
for idx = 1:num
    tdlen_e(idx) = size(td_e_ft{idx},2);
end

save('tdarr_a2h','td_e','tdlen_e','-append');

%%
td_f = [td_f_ft{:}];
num = size(td_f_ft,2);
tdlen_f = zeros(1,num);
for idx = 1:num
    tdlen_f(idx) = size(td_f_ft{idx},2);
end

save('tdarr_a2h','td_f','tdlen_f','-append');

%%
td_g = [td_g_ft{:}];
num = size(td_g_ft,2);
tdlen_g = zeros(1,num);
for idx = 1:num
    tdlen_g(idx) = size(td_g_ft{idx},2);
end

save('tdarr_a2h','td_g','tdlen_g','-append');

%%
td_h = [td_h_ft{:}];
num = size(td_h_ft,2);
tdlen_h = zeros(1,num);
for idx = 1:num
    tdlen_h(idx) = size(td_h_ft{idx},2);
end

save('tdarr_a2h','td_h','tdlen_h','-append');

%%
td_i = [td_i_ft{:}];
num = size(td_i_ft,2);
tdlen_i = zeros(1,num);
for idx = 1:num
    tdlen_i(idx) = size(td_i_ft{idx},2);
end

save('tdarr_a2h','td_i','tdlen_i','-append');

%%
td_j = [td_j_ft{:}];
num = size(td_j_ft,2);
tdlen_j = zeros(1,num);
for idx = 1:num
    tdlen_j(idx) = size(td_j_ft{idx},2);
end

save('tdarr_a2h','td_j','tdlen_j','-append');

%%
td_k = [td_k_ft{:}];
num = size(td_k_ft,2);
tdlen_k = zeros(1,num);
for idx = 1:num
    tdlen_k(idx) = size(td_k_ft{idx},2);
end

save('tdarr_a2h','td_k','tdlen_k','-append');

%%
td_l = [td_l_ft{:}];
num = size(td_l_ft,2);
tdlen_l = zeros(1,num);
for idx = 1:num
    tdlen_l(idx) = size(td_l_ft{idx},2);
end

save('tdarr_a2h','td_l','tdlen_l','-append');

%%
td_m = [td_m_ft{:}];
num = size(td_m_ft,2);
tdlen_m = zeros(1,num);
for idx = 1:num
    tdlen_m(idx) = size(td_m_ft{idx},2);
end

save('tdarr_a2h','td_m','tdlen_m','-append');

%%
td_n = [td_n_ft{:}];
num = size(td_n_ft,2);
tdlen_n = zeros(1,num);
for idx = 1:num
    tdlen_n(idx) = size(td_n_ft{idx},2);
end

save('tdarr_a2h','td_n','tdlen_n','-append');

%%
td_o = [td_o_ft{:}];
num = size(td_o_ft,2);
tdlen_o = zeros(1,num);
for idx = 1:num
    tdlen_o(idx) = size(td_o_ft{idx},2);
end

save('tdarr_a2h','td_o','tdlen_o','-append');

%%
td_p = [td_p_ft{:}];
num = size(td_p_ft,2);
tdlen_p = zeros(1,num);
for idx = 1:num
    tdlen_p(idx) = size(td_p_ft{idx},2);
end

save('tdarr_a2h','td_p','tdlen_p','-append');

%%
td_q = [td_q_ft{:}];
num = size(td_q_ft,2);
tdlen_q = zeros(1,num);
for idx = 1:num
    tdlen_q(idx) = size(td_q_ft{idx},2);
end

save('tdarr_a2h','td_q','tdlen_q','-append');

%%
td_r = [td_r_ft{:}];
num = size(td_r_ft,2);
tdlen_r = zeros(1,num);
for idx = 1:num
    tdlen_r(idx) = size(td_r_ft{idx},2);
end

save('tdarr_a2h','td_r','tdlen_r','-append');

%%
td_s = [td_s_ft{:}];
num = size(td_s_ft,2);
tdlen_s = zeros(1,num);
for idx = 1:num
    tdlen_s(idx) = size(td_s_ft{idx},2);
end

save('tdarr_a2h','td_s','tdlen_s','-append');

%%
td_t = [td_t_ft{:}];
num = size(td_t_ft,2);
tdlen_t = zeros(1,num);
for idx = 1:num
    tdlen_t(idx) = size(td_t_ft{idx},2);
end

save('tdarr_a2h','td_t','tdlen_t','-append');

%%
td_u = [td_u_ft{:}];
num = size(td_u_ft,2);
tdlen_u = zeros(1,num);
for idx = 1:num
    tdlen_u(idx) = size(td_u_ft{idx},2);
end

save('tdarr_a2h','td_u','tdlen_u','-append');

%%
td_v = [td_v_ft{:}];
num = size(td_v_ft,2);
tdlen_v = zeros(1,num);
for idx = 1:num
    tdlen_v(idx) = size(td_v_ft{idx},2);
end

save('tdarr_a2h','td_v','tdlen_v','-append');

%%
td_w = [td_w_ft{:}];
num = size(td_w_ft,2);
tdlen_w = zeros(1,num);
for idx = 1:num
    tdlen_w(idx) = size(td_w_ft{idx},2);
end

save('tdarr_a2h','td_w','tdlen_w','-append');

%%
td_x = [td_x_ft{:}];
num = size(td_x_ft,2);
tdlen_x = zeros(1,num);
for idx = 1:num
    tdlen_x(idx) = size(td_x_ft{idx},2);
end

save('tdarr_a2h','td_x','tdlen_x','-append');

%%
td_y_curly = [td_y_curly_ft{:}];
num = size(td_y_curly_ft,2);
tdlen_y_curly = zeros(1,num);
for idx = 1:num
    tdlen_y_curly(idx) = size(td_y_curly_ft{idx},2);
end

save('tdarr_a2h','td_y_curly','tdlen_y_curly','-append');

%%
td_z = [td_z_ft{:}];
num = size(td_z_ft,2);
tdlen_z = zeros(1,num);
for idx = 1:num
    tdlen_z(idx) = size(td_z_ft{idx},2);
end

save('tdarr_a2h','td_z','tdlen_z','-append');
clear